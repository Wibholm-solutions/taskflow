name: taskflow
status: active
type: service
description: "Personal task management PWA with recurring tasks, swipe gestures, and offline support"

critical_paths:
  - docker-compose.yml
  - server/src/
  - client/src/
  - CLAUDE.md

safe_to_delete:
  - "*.log"
  - "dist/"
  - "node_modules/"

dependencies:
  - caddy
